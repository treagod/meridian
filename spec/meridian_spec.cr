require "./spec_helper"

describe "Meridian::CLI" do
  describe "global flags" do
    pending "exits with code 0 when --help is passed"
    pending "prints usage information when --help is passed"
    pending "prints the version string when --version is passed"
    pending "exits with code 0 when --version is passed"
  end

  describe "subcommands" do
    pending "prints 'Not yet implemented' for the deploy subcommand"
    pending "prints 'Not yet implemented' for the setup subcommand"
    pending "prints 'Not yet implemented' for the rollback subcommand"
    pending "prints 'Not yet implemented' for the status subcommand"
    pending "prints 'Not yet implemented' for the logs subcommand"
    pending "exits with a non-zero code for unknown subcommands"
    pending "prints an error message for unknown subcommands"
  end
end
