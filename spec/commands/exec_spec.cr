require "../spec_helper"

describe "Meridian::Commands::Exec" do
  describe "#run" do
    pending "runs the given command inside the running container via podman exec"
    pending "raises an error when the role does not exist"
  end
end
