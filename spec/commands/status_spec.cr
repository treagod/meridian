require "../spec_helper"

describe "Meridian::Commands::Status" do
  describe "#run" do
    pending "queries systemctl status on all web hosts"
    pending "queries all roles, not only web"
  end
end
