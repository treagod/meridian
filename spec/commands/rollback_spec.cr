require "../spec_helper"

describe "Meridian::Commands::Rollback" do
  describe "#run" do
    pending "reads the active colour from .meridian-color on each host"
    pending "switches kamal-proxy back to the inactive colour"
    pending "raises RollbackFailed when the inactive container is no longer present"
  end
end
