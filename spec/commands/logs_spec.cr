require "../spec_helper"

describe "Meridian::Commands::Logs" do
  describe "#run" do
    pending "runs journalctl on the specified host"
    pending "passes the follow flag to journalctl"
    pending "filters journalctl output by the service name"
  end
end
